name: simple_logging
description: simple logging package, with log level / level prefix / logging call position info features.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
license: MIT

environment:
  sdk: ^3.5.1

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
